export interface IMainCharacterInfo {
  pos : Object;
  // pos.x : number;
  // position : Object;
  // pos : this.position;
  // pos : { [x: string] : number, [y: number] : number}
  // position: { [x: string] : number };
  // position: { [y: string] : number };
}
